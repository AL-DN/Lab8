// connects the pieces together
import { StrictMode } from 'react';                 // react
import { createRoot } from 'react-dom/client';      // react library
import './styles.css';                              // styles for components

import App from './App';                            // code from app.js